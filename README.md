# DDS Data Vault на Trino

## 📋 Описание проекта
Проект реализует слой DDS (Detail Data Storage) по методологии **Data Vault 2.0** с использованием распределенного движка **Trino** и данных **TPC-H**. 

## 📊 Технологический стек
* **Trino** — распределенный SQL-движок
* **TPC-H** — источник данных (схема `tiny`)
* **Data Vault 2.0** — методология моделирования (Hubs, Links, Satellites)

### 🎯 Хаб-таблицы (Hubs)
* `hub_order` — заказы
* `hub_customer` — клиенты
* `hub_part` — детали (товары)
* `hub_supplier` — поставщики
* `hub_nation` — страны
* `hub_region` — регионы

### 🔗 Линк-таблицы (Links)
* `lnk_customer_order` — связь "Клиент — Заказ"
* `lnk_nation_region` — связь "Страна — Регион"
* **`lnk_order_part_supplier`** — транзакционная связь "Заказ — Товар — Поставщик"

### 📈 Сателлит-таблицы (Satellites)
* `sat_order` — атрибуты заголовка заказа (статус, приоритет)
* `sat_customer`, `sat_part`, `sat_supplier` — описательные атрибуты справочников
* `sat_nation`, `sat_region` — справочники географии
* **`sat_order_part_supplier`** — детали позиции заказа (цена, количество, скидка) — **Satellite on Link**

### 3. Запуск скриптов
* **`01_init_ddl.sql`** — создание структуры
* **`02_load_data.sql`** — полная начальная загрузка
* **`03_load_date_data.sql`** — загрузка за конкретный день
